<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Auxiliary" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CYActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:displayOptions">showCustom</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetLeft">0dp</item>
        <item name="contentInsetRight">0dp</item>
        <item name="contentInsetEnd">0dp</item>
        <item name="displayOptions">showCustom</item>
        <item name="actionBarSize">88dp</item>
        <item name="android:actionBarSize">88dp</item>

    </style>

    <style name="Theme.Auxiliary" parent="Base.Theme.Auxiliary" >
        <item name="android:actionBarStyle">@style/CYActionBar</item>
        <item name="actionBarStyle">@style/CYActionBar</item>

    </style>
    <style name="Theme.Auxiliary_NoActionBar" parent="Theme.Material3.DayNight.NoActionBar" >
    </style>

</resources>